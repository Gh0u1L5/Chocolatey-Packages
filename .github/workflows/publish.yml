on: [push]
jobs:
  publish:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Pack
        uses: crazy-max/ghaction-chocolatey@v2.0.0
        with:
          args: pack